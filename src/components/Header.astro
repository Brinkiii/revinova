---
import headerBild from "../images/header.jpg";
import { Image } from 'astro:assets';
import Navbar from "./Navbar.astro";
---

<header class="min-h-96 lg:h-full">
	<Navbar/>
    <div class="grid grid-cols-1 lg:grid-cols-12 px-4 lg:px-8 gap-4 mt-[136px]">
        <div class="lg:col-span-6">
            <Image src={headerBild} alt="Header" class="object-cover"/>
        </div>
        <div class="lg:col-span-6">
            <Image src={headerBild} alt="Header" class="object-cover"/>
        </div>
    </div>
</header>
<style>
    header {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
        /* background-image: url(/src/images/header.jpg);
        background-size: cover; */
    }
    /* header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(255, 255, 255, .5);
        z-index: 10;
    } */
</style>